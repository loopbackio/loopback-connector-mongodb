{
  "extends": "loopback",
  "rules": {
    "max-len": ["error", 80, 4, {
      "ignoreComments": true,
      "ignoreUrls": true,
      "ignorePattern": "^\\s*var\\s.+=\\s*(require\\s*\\()|(/)"
    }]
   }
}